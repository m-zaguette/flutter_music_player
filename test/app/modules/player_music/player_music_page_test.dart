import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:flutter_agettzue/app/modules/player_music/player_music_page.dart';

void main() {
  testWidgets('PlayerMusicPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PlayerMusicPage(title: 'PlayerMusic')));
    //  final titleFinder = find.text('PlayerMusic');
    //  expect(titleFinder, findsOneWidget);
  });
}
